# Local development placeholders only. Do not put production secrets here.
POSTGRES_DB=lucid_hub
POSTGRES_USER=lucid
POSTGRES_PASSWORD=replace-with-local-dev-password

# Public client API origin for local web/admin development.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4000

# Development-only switch for preview request admin endpoints. Not production auth.
PREVIEW_ADMIN_ENABLED=false
